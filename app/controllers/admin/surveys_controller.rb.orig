class Admin::SurveysController < ApplicationController
  def new
    @survey = Survey.new
  end

  def index
    @surveys = Survey.all
  end

  def create
    @survey = Survey.new(survey_params)
    if @survey.save
      flash[:success] = "Success! Your survey '#{@survey.title}' has been created!"
      redirect_to [:admin, @survey]
    else
      flash[:error] = "Oops! Please check your form for errors"
      render :new
    end
  end

  def show
<<<<<<< HEAD
    session[:question_params] = session[:form_step] = nil
    @survey = Survey.find(params[:id])
    @questions = @survey.questions
=======
    @survey = Survey.find(params[:id])
>>>>>>> a4295d0... Save work in progress
  end

  def questiontype
    @survey = Survey.find(params[:id])
    if valid_qtype?
<<<<<<< HEAD
      session[:qtype] = params['qtype']
      return redirect_to [:new, :admin, @survey, :question]
=======
      # store questiontype (Option / Num) in session:qtype]
      session[:qtype] = params['qtype']
      return redirect_to [:new, :admin, @survey, :question]
      #redirect_to [:build, :admin, @survey]
>>>>>>> a4295d0... Save work in progress
    else
      render :show
    end
  end

<<<<<<< HEAD
  def results
    @survey = Survey.find(params[:id])
=======
  def build
    @survey = Survey.find(params[:id])

>>>>>>> a4295d0... Save work in progress
  end

  private

  def survey_params
    params.require(:survey).permit(:title, :description)
  end

  def valid_qtype?
    params['qtype'] == 'Option' || params['qtype'] == 'Num'
  end
end
